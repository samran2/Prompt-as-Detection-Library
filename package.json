{
  "name": "prompt-as-detection-library",
  "version": "0.3.0-dev.2",
  "private": true,
  "license": "MIT",
  "description": "Independent local prompt library for every active MITRE ATT&CK 19.2 technique and subtechnique",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check demo/core.js && node --check demo/catalog.js && node --check demo/app.js && node --check scripts/build_library.cjs && node --check scripts/library_cli.cjs && node --check scripts/build_demo.cjs",
    "library:build": "node scripts/build_library.cjs",
    "library:verify": "node scripts/build_library.cjs --check",
    "library:help": "node scripts/library_cli.cjs --help",
    "build": "npm run library:verify && node scripts/build_demo.cjs"
  }
}
